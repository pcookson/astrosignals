services:
  api:
    build: ./apps/api
    ports:
      - "8000:8000"

  web:
    build: ./apps/web
    ports:
      - "5173:5173"
    depends_on:
      - api
    environment:
      - VITE_API_BASE_URL=http://api:8000
